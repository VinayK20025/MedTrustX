# =============================================================================
# MedTrustX AI Platform Service — Python Dependencies
# =============================================================================
# Python 3.11 | FastAPI async service for AI inference & analytics
# Pin every package to a specific version for reproducible production builds.
# =============================================================================

# ─── Web Framework ───────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
httpx==0.27.0
websockets==12.0

# ─── Pydantic v2 (strict models, settings) ───────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1

# ─── Async PostgreSQL (SQLAlchemy 2 + asyncpg) ────────────────────────────────
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0

# ─── Redis (async, pub/sub) ───────────────────────────────────────────────────
redis[hiredis]==5.0.4

# ─── HTTP Client (async — for TF Serving REST + Keycloak JWKS) ───────────────
aiohttp==3.9.5

# ─── Authentication & Cryptography ───────────────────────────────────────────
# RS256 JWT validation
python-jose[cryptography]==3.3.0
# PyNaCl for additional cryptographic primitives
PyNaCl==1.5.0
# Open Quantum Safe — Kyber-768 PQC key encapsulation
# liboqs-python must be installed from source in Dockerfile;
# the PyPI stub is listed here for dependency tracking.
liboqs-python==0.10.0
# Standard cryptography library
cryptography==42.0.7
# JWKS fetching + caching
cachetools==5.3.3

# ─── ML / Inference ───────────────────────────────────────────────────────────
numpy==1.26.4
scipy==1.13.0
scikit-learn==1.4.2
# SHAP for model explainability
shap==0.45.1
# MLflow client for model registry
mlflow==2.13.0
# grpcio for TF Serving gRPC (REST is primary; gRPC is fallback)
grpcio==1.64.0
grpcio-tools==1.64.0

# ─── Observability ────────────────────────────────────────────────────────────
# OpenTelemetry
opentelemetry-api==1.24.0
opentelemetry-sdk==1.24.0
opentelemetry-exporter-otlp-proto-http==1.24.0
opentelemetry-instrumentation-fastapi==0.45b0
opentelemetry-instrumentation-sqlalchemy==0.45b0
opentelemetry-instrumentation-redis==0.45b0
opentelemetry-instrumentation-httpx==0.45b0
# Prometheus metrics
prometheus-client==0.20.0
# Structured JSON logging
python-json-logger==2.0.7

# ─── Data Processing / Feature Engineering ───────────────────────────────────
pandas==2.2.2

# ─── Async Utilities ─────────────────────────────────────────────────────────
anyio==4.3.0
tenacity==8.3.0

# ─── Testing ─────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
httpx==0.27.0
# async DB fixture support
pytest-postgresql==5.1.0

# ─── Code Quality (dev only, included for CI gate) ───────────────────────────
ruff==0.4.4
