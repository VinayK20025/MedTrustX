'use client';
import React from 'react';
import { Breadcrumbs } from '@/components/ui/Breadcrumbs';
import { Card, CardBody } from '@/components/ui/Card';

export default function PatientCounselorReportsPage() {
  return (
    <div className="space-y-5 animate-fade-in max-w-[1600px]">
      <Breadcrumbs items={[{ label: 'Patient Counseling' }, { label: 'Counseling Logs' }]} />
      <Card className="border-white/[0.06] shadow-glass bg-surface-light h-full min-h-[400px] flex items-center justify-center">
        <CardBody className="text-gray-500 text-center">
          <p className="text-lg">Session Analytics</p>
          <p className="text-sm">Export patient satisfaction scores and conversion rates for elective procedures.</p>
        </CardBody>
      </Card>
    </div>
  );
}
