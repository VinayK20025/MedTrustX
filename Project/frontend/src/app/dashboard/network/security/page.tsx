'use client';
import React from 'react';
import { Breadcrumbs } from '@/components/ui/Breadcrumbs';
import { Card, CardBody } from '@/components/ui/Card';

export default function NetworkSecurityPage() {
  return (
    <div className="space-y-5 animate-fade-in max-w-[1200px]">
      <Breadcrumbs items={[{ label: 'IT Administration' }, { label: 'Edge Security' }]} />
      <Card className="border-white/[0.06] shadow-glass bg-surface-light min-h-[400px] flex items-center justify-center">
        <CardBody className="text-gray-500 text-center">
          <p className="text-lg">Firewall Rules & IDS Logs</p>
          <p className="text-sm">Monitor edge threats, unauthorized port scans, and DDoS mitigation.</p>
        </CardBody>
      </Card>
    </div>
  );
}
