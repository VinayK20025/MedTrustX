'use client';
import React from 'react';
import { Breadcrumbs } from '@/components/ui/Breadcrumbs';
import { Card, CardBody } from '@/components/ui/Card';

export default function ElectricianSystemsPage() {
  return (
    <div className="space-y-5 animate-fade-in max-w-[1200px]">
      <Breadcrumbs items={[{ label: 'Facility' }, { label: 'Power Systems' }]} />
      <Card className="border-white/[0.06] shadow-glass bg-surface-light min-h-[400px] flex items-center justify-center">
        <CardBody className="text-gray-500 text-center">
          <p className="text-lg">Electrical Systems Monitor</p>
          <p className="text-sm">Track loads and statuses for backup generators, UPS units, and breaker panels.</p>
        </CardBody>
      </Card>
    </div>
  );
}
